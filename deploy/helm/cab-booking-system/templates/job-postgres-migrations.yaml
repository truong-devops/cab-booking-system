{{- if .Values.jobs.postgresMigrations.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: cab-apply-postgres-migrations
  namespace: {{ .Values.global.namespace | quote }}
  labels:
    {{- include "cab.labels" . | nindent 4 }}
  annotations:
    {{- include "cab.hookAnnotations" (dict "weight" "-30") | nindent 4 }}
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cab-apply-postgres-migrations
        app.kubernetes.io/instance: {{ .Release.Name | quote }}
    spec:
      restartPolicy: OnFailure
      serviceAccountName: {{ include "cab.serviceAccountName" . | quote }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
      {{- range . }}
        - name: {{ . | quote }}
      {{- end }}
      {{- end }}
      containers:
        - name: apply-migrations
          image: {{ include "cab.image" (dict "root" . "image" .Values.jobs.postgresMigrations.image) | quote }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy | quote }}
          command:
            - /bin/sh
            - /docker-entrypoint-initdb.d/02-apply-service-schemas.sh
          env:
            {{- include "cab.envVars" (dict "plain" .Values.jobs.postgresMigrations.env "secret" .Values.jobs.postgresMigrations.secretEnv "secretName" .Values.global.secretName) | nindent 12 }}
          securityContext:
            {{- toYaml .Values.global.containerSecurityContext | nindent 12 }}
{{- end }}
